{
  "name": "message_in_a_bottle",
  "version": "0.0.1",
  "description": "",
  "dependencies": {
    "@tensorflow-models/toxicity": "1.2.2",
    "@tensorflow/tfjs": "^1.3.3"
  },
  "scripts": {
    "dev": "parcel src/index.html --open ",
    "pages-dev": "rimraf dist && yarn build && wrangler pages dev dist --local --live-reload",
    "build": "parcel build src/index.html",
    "lint": "eslint .",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.8.1",
    "buffer": "^5.5.0",
    "eslint": "8.29.0",
    "parcel": "2.8.1",
    "process": "0.11.10",
    "rimraf": "3.0.2",
    "typescript": "4.9.4",
    "wrangler": "2.6.1"
  }
}
